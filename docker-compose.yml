services:
  postgres:
    image: postgres:17
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: granith
      POSTGRES_PASSWORD: granith
      POSTGRES_DB: granith
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
