# Base URL for the backend API. Leave empty in development so the Vite proxy
# in vite.config.ts handles /api requests against http://localhost:3000.
#
# For production builds, set this to the absolute origin of the API host:
#   PUBLIC_API_BASE_URL=https://api.granith.dev
PUBLIC_API_BASE_URL=
